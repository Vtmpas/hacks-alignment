import pathlib

BOT_DIR = pathlib.Path(__file__).parent.absolute()
LOG_DIR = BOT_DIR / "logs"

# BOT_DIR: Absolute path to the directory containing the current script file.
# LOG_DIR: Path to the "logs" directory within the bot's directory.
